<UserControl x:Class="PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             FontFamily="{StaticResource ArabicFont}"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Payment Entry Card -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="سند قبض وتسديد من طالب (إيداع في الخزينة)" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                    </Grid.ColumnDefinitions>

                    <!-- Student Dropdown -->
                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="اختر الطالب المسدد *" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <ComboBox x:Name="cmbStudents" Style="{StaticResource ModernComboBoxStyle}" SelectionChanged="cmbStudents_SelectionChanged" DisplayMemberPath="DisplayName" SelectedValuePath="StudentID" />
                    </StackPanel>

                    <!-- Amount Paid -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="المبلغ المقبوض (د.ل) *" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtPaidAmount" Style="{StaticResource ModernTextBoxStyle}" Height="38" Text="0.00" />
                    </StackPanel>
                </Grid>

                <!-- Student Current Debt Banner -->
                <Border x:Name="pnlStudentDebtBanner" Background="#ECFDF5" CornerRadius="8" Padding="12" Margin="0,16,0,16" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💵 الدين المتبقي على الطالب حالياً: " FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock x:Name="txtStudentDebt" Text="0.00 د.ل" FontSize="14" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" Margin="8,0,0,0" />
                    </StackPanel>
                </Border>

                <!-- Notes & Submit -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="بيان سند القبض / رقم الوصل" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtNotes" Style="{StaticResource ModernTextBoxStyle}" Height="38" Text="سداد من حساب الطالب" />
                    </StackPanel>

                    <Button Grid.Column="1" x:Name="btnSavePayment" Content="طباعة وحفظ سند القبض 💵" Style="{StaticResource PrimaryButtonStyle}" Height="38" Width="220" VerticalAlignment="Bottom" Click="btnSavePayment_Click" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Payments History Grid -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="سجل المقبوضات والسندات الأخيرة" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />

                <DataGrid Grid.Row="1" x:Name="dgPayments" Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم السند" Binding="{Binding TransactionID}" Width="90" />
                        <DataGridTextColumn Header="اسم الطالب المسدد" Binding="{Binding StudentName}" Width="*" />
                        <DataGridTextColumn Header="المبلغ المقبوض (دائن)" Binding="{Binding Credit, StringFormat={}{0:N2}}" Width="150" />
                        <DataGridTextColumn Header="البيان والملاحظات" Binding="{Binding Notes}" Width="2*" />
                        <DataGridTextColumn Header="تاريخ القبض" Binding="{Binding TransactionDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="150" />
                        <DataGridTextColumn Header="الموظف المستقبل" Binding="{Binding Username}" Width="130" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
