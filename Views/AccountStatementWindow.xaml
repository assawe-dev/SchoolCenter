<Window x:Class="AccountStatementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="كشف حساب تفصيلي للطالب"
        MinHeight="550" MinWidth="850"
        Height="650" Width="1000"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="{StaticResource ArabicFont}"
        Loaded="Window_Loaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="txtStudentTitle" Text="كشف حساب الطالب: " FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="عرض التسلسل الزمني لجميع الحركات المالية والرصيد المتبقي التراكمي" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
        </StackPanel>

        <!-- Summary Cards Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Card 1: Total Required (Debit) -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="1" Padding="14" Margin="0,0,8,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="إجمالي المطلوب (مدين)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock x:Name="txtTotalDebit" Text="0.00 د.ل" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Card 2: Total Paid (Credit) -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="1" Padding="14" Margin="4,0,4,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="إجمالي المدفوع (دائن)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock x:Name="txtTotalCredit" Text="0.00 د.ل" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Card 3: Final Balance -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="1" Padding="14" Margin="8,0,0,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="الرصيد المتبقي النهائي" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock x:Name="txtFinalBalance" Text="0.00 د.ل" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- DataGrid Statement -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid x:Name="dgStatement" Style="{StaticResource ModernDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="تاريخ الحركة" Binding="{Binding TransactionDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="1.3*" MinWidth="130" />
                    <DataGridTextColumn Header="نوع الحركة" Binding="{Binding TranslatedType}" Width="1.2*" MinWidth="110" />
                    <DataGridTextColumn Header="البيان / ملاحظات" Binding="{Binding Notes}" Width="2.5*" MinWidth="180" />
                    <DataGridTextColumn Header="المطلوب (مدين)" Binding="{Binding Debit, StringFormat={}{0:N2}}" Width="*" MinWidth="100" />
                    <DataGridTextColumn Header="المدفوع (دائن)" Binding="{Binding Credit, StringFormat={}{0:N2}}" Width="*" MinWidth="100" />
                    <DataGridTextColumn Header="الرصيد التراكمي" Binding="{Binding RunningBalance, StringFormat={}{0:N2}}" Width="1.1*" MinWidth="110" />
                    <DataGridTextColumn Header="الموظف" Binding="{Binding Username}" Width="*" MinWidth="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="btnPrintStatement" Content="🖨 طباعة كشف الحساب" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Click="btnPrintStatement_Click" />
            <Button x:Name="btnExportStatement" Content="تصدير Excel" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Click="btnExportStatement_Click" />
            <Button Content="× إغلاق" Style="{StaticResource SecondaryButtonStyle}" Click="btnClose_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>